{% extends 'base.html.twig' %}

{% block title %}Annonce
{% endblock %}

{% block body %}


	<div class="container">
		<h3>
			<ta-center>Venez découvrir toutes nos annonces</ta-center>
		</h3><br/><br/>
		
		<div class="row">
			{% for annonce in annonces %}

				<div class="col-md-4">
					<div class="card mb-3">
						<h3 class="card-header text-center">
							{% if annonce.rooms < 2 %}Une chambre
							{% else %}
								{{annonce.rooms}}
								Chambres
							{% endif %}
							à
							{{annonce.price | number_format(0, ',', ' ')}}
							/mois
							<br/>
							<small>{{annonce.comments |length }}
								commentaires</small>
						</h3>

						<img src="{{ asset('uploads/coverImage/' ~ annonce.coverImage )}}" alt="image" style="height:300; width:100%;">

						<div class="card-body">


							<h1>{{annonce.title}}</h1>

							<h3 class="card-text">{{annonce.introduction |u.truncate(100, '...')}}</h3>
						</div>

						<div class="card-body">
							<a href="{{ path('annonce_show', {'slug':annonce.slug}) }}" class="card-link btn btn-primary">Détails...</a>
						</div>
						<div class="card-footer text-muted text-center">
							<i class="fas fa-map-marker">
								{{annonce.address}}
							</i>
							<br>
							<small>{{annonce.createdAt |ago(locale='fr')}}</small>
						</div>
					</div>

				</div>
			{% endfor %}
		</div>
	</div>


{% endblock %}

